# URL of the infoclimat-om-worker (cumulative precipitation, etc.).
# When unset, cumul variables fall back to upstream Open-Meteo (which returns 404).
# Local dev: VITE_OM_WORKER_URL=http://localhost:8080
VITE_OM_WORKER_URL=
